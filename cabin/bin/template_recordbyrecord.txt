from biodb.data.db import RecordByRecordImportedTable
from biodb.data.files import LocalFile, ExternalFile


class {name}Official(ExternalFile):
    version = '----'

    @property
    def url(self):
        return '----'.format(version=self.version)


class {name}File(LocalFile):
    version = '1'
    depends = [{name}Official]
    extension = 'txt'


class {name}Table(RecordByRecordImportedTable):
    version = ' 1'
    depends = [{name}File]

    columns = [
        '----'
    ]

    @property
    def schema(self):
        return'---'

    def read(self):
        for row in '----'

